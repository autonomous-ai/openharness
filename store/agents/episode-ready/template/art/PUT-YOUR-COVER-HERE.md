Drop the episode or show artwork here, as `.png` or `.jpg`.

Apple Podcasts wants it square, RGB, between 1400×1400 and 3000×3000 pixels. Anything square and
big enough works; Episode Ready writes both sizes into `dist/` and embeds the cover in the audio.
